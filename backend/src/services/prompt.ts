// 业务约束 prompt（附录A）。每次调用阿里云原样附加。
// 修改这段直接影响识别效果，改动需谨慎并配合诊断页回归。
export const VISION_PROMPT = `请分析这张图片，判断它是哪个中文社交平台的评论截图，并提取所有可见的【顶级评论】（不要提取子回复）。

# 三平台判断规则（精简版，只用3条核心信号）

▼ Bilibili 判断条件（优先判断）
- 用户名所在行包含 "LV1"~"LV6" 等级标识
- 评论下方有独立时间地点行，格式：X月X日 + 地区 + 回复（如"1月23日 广东 回复"）
- 命中即判定 bilibili，置信度0.95

▼ 视频号 判断条件
- 用户名 + 地区 + 日期 在同一行（如"墨玉白兰 青海 3月4日"、"YU维斯塔潘UY 福建 17天前"）
- 评论下方没有独立时间地点行
- 没有"回复"字样紧跟时间地点
- 没有 LV 等级
- 命中即判定 video_channel，置信度0.9

▼ 小红书 判断条件
- 用户名独占一行（无等级、无地区、无日期）
- 评论下方有独立时间地点行：时间+地点+回复
- 时间格式：X分钟前/X小时前/X天前/昨天HH:MM/X月X日（如"2小时前 江苏 回复"、"59分钟前 浙江 回复"）
- 没有 LV 等级
- 命中即判定 xiaohongshu，置信度0.9

判断顺序：
1. 先扫描 LV 等级 → 命中 → bilibili
2. 再扫描"用户名+地区+日期同行"模式 → 命中 → video_channel
3. 再扫描"用户名独占行+评论+时间地点+回复"模式 → 命中 → xiaohongshu
4. 都没命中 → unknown

⚠️ 不要用以下不稳定特征做判断（已验证不可靠）：
- 输入框占位符文案（会变、滚动后消失）
- 底色（黑/白随形态变化）
- 顶部标题（截图可能是中段）
- "都在搜:"、"作者赞过"、"点我发弹幕"、"UP主"、"糕丝"等元素（不一定每张图都有）

# 字段提取规则

▼ username
- 视频号：第一行，地区/日期之前的部分（如"墨玉白兰 青海 3月4日 48"→"墨玉白兰"）
- 小红书：用户名行去除首尾空白
- Bilibili：第一行去掉LV标识（如"Qiu256 LV6"→"Qiu256"）

▼ content
- 用户名行和时间地点行之间所有文本，多行用空格连接
- 保留emoji原字符
- 不包含"回复"二字、不包含点赞数

▼ time_text
- 视频号：用户名同行中"用户名"之后、点赞数之前（地区+日期）
- 小红书：评论下方时间地点行去除"回复"二字（如"2小时前 江苏 回复"→"2小时前 江苏"）
- Bilibili：评论下方时间地点行去除"回复"二字（如"1月23日 广东 回复"→"1月23日 广东"）

# 必须忽略
- 顶部状态栏（时间/电量/信号）
- 顶部tab（"简介 评论"、"继续播放"等）
- 底部输入框
- 点赞数字
- 子回复内容
- 折叠按钮文字（"展开 X 条回复"、"共 X 条回复 ›"）
- 图片描述

# 严格输出格式（仅JSON，无markdown围栏，无解释文字）

{
  "platform": "xiaohongshu" | "video_channel" | "bilibili" | "unknown",
  "platform_confidence": 0.0-1.0,
  "platform_reason": "命中的关键特征描述（中文，简短）",
  "comments": [
    {"username": "...", "content": "...", "time_text": "..."}
  ]
}

异常处理：
- 图片不是评论列表：{"platform":"unknown","platform_confidence":0,"platform_reason":"非评论列表截图","comments":[]}
- 强特征都没命中：platform="unknown"，置信度<0.5
- 字段无法识别：value填空字符串""，不要编造

严格要求：回复必须以 { 开始、以 } 结束。不要markdown代码围栏（不要\`\`\`json或\`\`\`）。不要任何JSON前后的解释、问候、致谢。`;
