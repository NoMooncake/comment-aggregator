# 评论聚合管理系统 · 运维手册

> 单人使用版本。部署在阿里云 ECS Ubuntu 22.04，Docker 单容器，单文件 SQLite。

## 访问信息

| 项 | 值 |
|---|---|
| 网址 | http://8.160.117.92 |
| 账号 | admin / admin123（仅你登，可保持默认；想改去设置页） |
| 服务器 SSH | root@8.160.117.92（默认端口 22，密码你那有） |
| 项目目录 | `/opt/comment-aggregator/` |
| 数据文件 | `/opt/comment-aggregator/data/prod.db` |
| 环境变量 | `/opt/comment-aggregator/.env` |

## 技术栈速查

- 容器：Docker + docker-compose v2，单容器多阶段构建
- 后端：Node 22 / Express / TypeScript，监听容器 3001 → 宿主 80
- 前端：React + Vite，build 后由后端托管静态文件
- 数据库：SQLite，挂在宿主 `./data/`
- AI 视觉：Kimi `moonshot-v1-32k-vision-preview`，OpenAI 兼容协议
- 认证：JWT + bcrypt，token 存 localStorage

API Key 只在服务器 `.env`，前端代码 / git 仓库 / 浏览器都看不到。

## 日常使用流程

1. 上传截图 → AI 解析（5-15 秒）
2. 确认页校对（AI 会偶尔识别错平台或漏字符，手动改下拉框 / 文本框即可）
3. 入库
4. 列表写回复
5. 导出 Excel 待执行清单
6. 助手按 Excel 执行完，回来标记"已执行"

详细界面操作见 `试用指南.md`。

---

# 运维操作（你日常会用到的）

## 看容器状态

```bash
cd /opt/comment-aggregator
docker compose ps
```

`STATUS` 应该是 `Up X (healthy)`。

## 看日志

```bash
# 最近 50 行
docker compose logs --tail=50

# 实时跟踪（Ctrl+C 退出）
docker compose logs -f
```

## 重启

```bash
docker compose restart
# 或更彻底（重建容器，不动数据）
docker compose up -d --force-recreate
```

数据库文件在 `./data/prod.db`，重启 / 重建容器都不会丢。

## 切换 AI Key / 模型 / 温度

```bash
cd /opt/comment-aggregator
nano .env
```

可改的关键变量：

| 变量 | 当前值 | 说明 |
|---|---|---|
| `AI_API_KEY` | sk-xxx | Kimi/Moonshot Key（也支持其它 OpenAI 兼容服务） |
| `AI_ENDPOINT` | https://api.moonshot.cn/v1/chat/completions | API URL |
| `AI_MODEL` | moonshot-v1-32k-vision-preview | 模型 ID |
| `AI_TEMPERATURE` | 0.1 | 越低越稳定；K2 系列必须 1 |
| `AI_TIMEOUT_MS` | 30000 | 超时（毫秒），慢模型加到 120000 |
| `AI_MAX_TOKENS` | 4096 | 单次输出上限 |
| `JWT_SECRET` | (随机串) | 改了会强制所有人重登 |

改完：

```bash
docker compose up -d
# 容器自动检测 .env 变化并重启（10 秒内）
```

⚠️ 切回阿里云通义千问：
```
AI_ENDPOINT=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
AI_MODEL=qwen-vl-max-latest
```

## 切换模型后建议跑一次诊断

```
http://8.160.117.92/debug/ai-test
```

上传任意截图，看第②区块 model 行确认切对了；看第④区块识别结果验证质量。

## 备份数据库

**手动一次性备份**：

```bash
docker compose exec app sqlite3 /app/backend/data/prod.db \
  ".backup /app/backend/data/backup-$(date +%F).db"

# 备份文件出现在 /opt/comment-aggregator/data/backup-YYYY-MM-DD.db
```

**自动每天备份（推荐配上）**：

```bash
cat > /etc/cron.d/comment-aggregator-backup <<'EOF'
0 3 * * * root cd /opt/comment-aggregator && docker compose exec -T app sqlite3 /app/backend/data/prod.db ".backup /app/backend/data/backup-$(date +\%F).db" && find /opt/comment-aggregator/data/ -name "backup-*.db" -mtime +30 -delete
EOF
```

每天凌晨 3 点自动备份，保留 30 天。

**恢复数据**：

```bash
docker compose down
cp /opt/comment-aggregator/data/backup-2026-05-18.db /opt/comment-aggregator/data/prod.db
docker compose up -d
```

## 查询数据库

```bash
# 进 sqlite shell（容器内）
docker compose exec app sqlite3 /app/backend/data/prod.db

# 常用查询
sqlite> .tables
sqlite> SELECT COUNT(*) FROM Comment WHERE status='pending';
sqlite> SELECT platform, COUNT(*) FROM Comment GROUP BY platform;
sqlite> SELECT * FROM Comment WHERE userNickname LIKE '%关键词%';
sqlite> .quit
```

## 删数据（误入库）

界面上目前没"删评论"按钮（产品 backlog 里）。先用 SQL：

```bash
# 删一条
docker compose exec app sqlite3 /app/backend/data/prod.db \
  "DELETE FROM Comment WHERE id=123;"

# 清空所有评论（保留账号）
docker compose exec app sqlite3 /app/backend/data/prod.db \
  "DELETE FROM Comment;"
```

---

# 故障处理

## 访问网站打不开

```bash
# 1. 容器还活着吗
docker compose ps

# 2. 没活就重启
docker compose up -d

# 3. 还不行看日志找错
docker compose logs --tail=100
```

## AI 识别报错

打开 `/debug/ai-test`，上传截图复现，看 5 个调试区块定位：

- 区块②看请求 URL / model 对不对
- 区块③看 HTTP 状态码：401/403 = Key 问题；429 = 限流；500+ = 服务端问题；超时 = 网络或模型慢
- 区块④看 AI 返回原文，如果模型返回了奇怪格式，调 prompt（见后）
- 区块⑤看清洗后 JSON 结构

## 改 prompt 调识别准确率

```bash
# 在你自己电脑（不是服务器）编辑 prompt
# 项目源码里 backend/src/services/prompt.ts
```

prompt 改动需要**重新 build 镜像并部署**：

```bash
# 在源码所在 Mac 上
docker buildx build --platform=linux/amd64 -t comment-aggregator:latest --load .
docker save comment-aggregator:latest -o comment-aggregator.tar
gzip comment-aggregator.tar
scp comment-aggregator.tar.gz root@8.160.117.92:/opt/comment-aggregator/

# 服务器
ssh root@8.160.117.92
cd /opt/comment-aggregator
gunzip -f comment-aggregator.tar.gz
docker load -i comment-aggregator.tar
docker compose up -d --force-recreate
```

---

# 待办（可选优化，不影响使用）

| 项 | 价值 | 工作量 |
|---|---|---|
| 加 HTTPS（域名 + Let's Encrypt） | 防中间人窃听 token | 1 小时 |
| 关掉 /debug/ai-test 外网访问 | 防外部探测 | 5 分钟（重 build） |
| 加 cron 自动备份 | 防数据丢 | 5 分钟（已给命令） |
| 加图片本地压缩 | 节省 AI token 费 | 2 小时（前端改） |
| 加软删除 + 评论删除 UI | 误入库容易处理 | 1 小时 |
| 加 prompt A/B 切换 | 模型比较时不用重 build | 半天 |

---

# 关键约束（别破坏）

- `.env` **不要 commit 到任何 git**（API Key 泄露事大）
- `DATABASE_URL` 必须是**绝对路径** `file:/app/backend/data/prod.db`
  - Prisma 解析相对路径以 `schema.prisma` 所在目录为基准，会绕过 volume 导致数据不持久。这个坑踩过了。
- 删除 `/opt/comment-aggregator/data/prod.db` 前**必须先备份**
- 改 `docker-compose.yml` 后必须 `docker compose up -d --force-recreate`（普通 restart 不会重读 compose 文件）

---

# 联系

代码作者：[你的联系方式]

有 bug、想加功能、或者想自己改代码，直接联系我。
